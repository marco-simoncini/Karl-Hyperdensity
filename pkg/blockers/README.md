# Blockers Package

Canonical blocker taxonomy definitions for Hyperdensity.

Phase 0 policy posture:
- restart-bound is blocking
- rollout-bound is blocking
- destructive migration-bound is blocking
- missing evidence is blocking
- recent correlated warning is blocking
- historical warning debt is non-blocking only under bounded warning policy
