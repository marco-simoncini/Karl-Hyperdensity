# KHR-VM-Y Windows READY Lifecycle Observation

Hyperdensity observes the lifecycle harness with `cellShellMaterializationStatus=windows-vm-ready-lifecycle`. ResourceLease apply and ResourcePort persistent loops remain false.
