# KHR-VM-S Existing Windows Asset Observation

Hyperdensity observes KHR-VM-S as an existing Windows asset intake signal. The selected Windows ISO is mapped to Cell/Shell materialization as install media readiness only, not as installed boot disk readiness.

ResourcePort and ResourceLease remain planning-only. `resourceLeaseApply=false` and `resourcePortPersistentLoop=false`.
