# Hyperdensity KVM strict boot observation (KHR-VM-M)

KVM permission remediation and strict boot retry are observed as smoke-only Cell/Shell
materialization. ResourcePort and ResourceLease remain planning-only.
