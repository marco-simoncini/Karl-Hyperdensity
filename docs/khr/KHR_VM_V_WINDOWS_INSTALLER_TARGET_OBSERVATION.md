# KHR-VM-V Windows Installer Target Observation

Hyperdensity observes Windows installer boot and install-target disk preparation. Cell/Shell materialization maps `windowsInstallerBootabilityLevel` (e.g. `firmware-visible`). ResourcePort/ResourceLease remain planning-only (`resourceLeaseApply=false`, `resourcePortPersistentLoop=false`).
