# KHR-VM-W Windows ISO EFI Boot Observation

Hyperdensity observes Windows ISO EFI boot remediation. Cell/Shell materialization maps `windowsInstallerBootabilityLevel` (e.g. `efi-bootloader-invoked`). ResourcePort/ResourceLease remain planning-only.
