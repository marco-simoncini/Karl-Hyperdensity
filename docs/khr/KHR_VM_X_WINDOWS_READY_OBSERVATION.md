# KHR-VM-X Windows VM READY Observation

Hyperdensity observes Windows READY operationalization with `cellShellMaterializationStatus=windows-vm-ready` when Installer evidence PASS. ResourceLease apply and ResourcePort persistent loops remain false.
