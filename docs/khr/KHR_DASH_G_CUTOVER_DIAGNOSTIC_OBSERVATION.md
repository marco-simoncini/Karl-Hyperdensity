# KHR-DASH-G — Default-provider cutover (Hyperdensity diagnostic observation)

Hyperdensity remains **diagnostic/governance only**; it is not the canonical VM surface.

KHR default-provider status (`khr-native` in eligible Karl Instance environments) is recorded as governance metadata only. Cell, Shell, ResourcePort, and SessionTarget identifiers remain projection metadata on synthetic Karl Instance rows.

**Invariants:** `resourceLeaseApply=false`, `resourcePortPersistentLoop=false`, no autonomous reconciliation loop.

Authoritative evidence: `KHR/evidence/latest/dashboard-karlinstance-cutover/hyperdensity-cutover-diagnostic-observation.json`.
