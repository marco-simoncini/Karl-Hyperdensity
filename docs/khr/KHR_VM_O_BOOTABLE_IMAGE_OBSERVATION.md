# Hyperdensity bootable image observation (KHR-VM-O)

Bootable image qualification maps the boot-probe serial readiness level to smoke-only Cell/Shell
materialization. ResourcePort and ResourceLease remain planning-only.
