apiVersion: v1
kind: ConfigMap
metadata:
  name: hyperdensity-controller-state
  namespace: karl-system
  labels:
    app.kubernetes.io/name: hyperdensity-controller
    reference_only: "true"
    not_production_install: "true"
data:
  state.json: |
    {"stateVersion":1,"controllerState":{"stateVersion":1},"idempotencyRecords":[],"claimBoundary":"reference seed state only"}
