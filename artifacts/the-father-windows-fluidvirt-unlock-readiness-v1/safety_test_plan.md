# Formal Safety Test Plan

Includes: unit, fixture replay, sidecar mock, guest mock, lab integration, chaos/negative, stale evidence, identity mismatch, kill switch, rollback, return-to-floor, audit immutability, no-mutation, and hard-disabled executor checks.

Split status:

- already implemented
- missing before first CPU lab apply proposal
- missing before any RAM path
