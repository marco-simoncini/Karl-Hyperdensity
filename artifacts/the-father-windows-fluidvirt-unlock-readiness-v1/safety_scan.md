# Safety Scan

Pattern scan is executed across changed files for secrets, deploy commands, forbidden scope markers, mutation-enabling flags, and mutating QMP command tokens.

Any matched forbidden terms must remain documentation-only in rejected/not-enabled context.
