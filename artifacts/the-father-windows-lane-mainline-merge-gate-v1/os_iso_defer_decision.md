# os_iso_defer_decision

- `osIsoMergeAllowed=false`
- OS-ISO is explicitly deferred in this gate
- no packaging/systemd/DaemonSet/substrate changes are allowed now
- OS-ISO evaluation resumes only after future runtime MVP and subsequent gate
