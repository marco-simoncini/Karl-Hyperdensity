# CPU Scale Up

`CPU_UP_CONFIRMED=false`

## Blocker

- `WINDOWS_HYPERDENSITY_PARITY_BLOCKED_BY_KUBEVIRT_RUNTIME`
- Target VM is halted; no live VMI/QMP runtime exists.

## Mutation attempt

None (blocked by preflight guard).
