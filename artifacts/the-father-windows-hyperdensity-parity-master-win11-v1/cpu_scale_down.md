# CPU Scale Down / Return to Floor

`CPU_DOWN_CONFIRMED=false`

## Blocker

CPU down proof is not executable because CPU up did not run and the VM is not in a live runtime state.

## Mutation attempt

None.
