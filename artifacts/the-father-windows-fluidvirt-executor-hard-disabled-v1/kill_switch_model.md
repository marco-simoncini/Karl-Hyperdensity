# Kill Switch Model

`WindowsFluidKillSwitch` is mandatory for future apply candidacy and defaults to hard-disabled policy mode.

If kill switch proof is missing/unobservable, evaluator returns blocked execution in this milestone.
