# Product Decision

Replay confirms that real `master-win11` evidence is classifiable by the compliance engine as `HYPERDENSITY_READY_WINDOWS_SHELL` under Prearmed Fluid Envelope v2 constraints:

- CPU liquidity via node-local actuator + `cgroup v2 cpu.max`
- RAM liquidity via QMP balloon
- no vCPU hotplug claim
- no logical CPU count scaling claim
- continuity invariants maintained

Pool decision:

- pool-child is allowed when treated as provisioning context only
- pool as scaling mechanism is blocked
