# pool_context_only_summary

Source reference reused from previous milestone log:

- `artifacts/the-father-windows-fluidvirt-runtime-gate-qmp-v1/build_or_test_logs/cluster-probe.log`

Observed context-only entities used for fixture shaping:

- `win11-pool-0`
- `win11-pool-1`

Dry-run rule:

- pool replicas remain context-only evidence.
- pool fixtures always classify as `BLOCKED_POOL_REPLICA_MODEL`.
- pool behavior is never accepted as FluidVirt success mechanism.
