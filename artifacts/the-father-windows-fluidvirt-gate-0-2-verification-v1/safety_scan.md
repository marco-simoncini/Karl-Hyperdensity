# Safety Scan

Pattern scan executed on changed files for secrets, deploy commands, forbidden scope touches, mutating QMP command references, and execution-enable flags.

Any forbidden terms in docs/fixtures/tests are acceptable only as rejected/not-executed contexts.
